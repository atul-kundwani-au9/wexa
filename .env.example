# In local dev the Vite dev server proxies /api to VITE_API_PROXY_TARGET
# (see vite.config.js), so you usually don't need this file locally.
#
# For a production build, set the deployed backend's base URL here so the
# built app calls it directly instead of a relative /api path:
VITE_API_BASE_URL=
